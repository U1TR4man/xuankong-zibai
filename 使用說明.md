# 玄空紫白 — 使用說明（不需要懂 coding）

這份說明假設你完全沒寫過程式。三種用法，由簡到繁。

---

## 一、自己先看看（最快，30 秒）

資料夾裡有一個檔案：

```
玄空紫白.html
```

**雙擊它**，就會用 Safari / Chrome 打開，可以直接排盤。

這個檔案是完整的 App，約 264 KB，什麼都包在裡面了 —— 節氣表、演算法、畫面全部都在。
**不用上網、不用裝任何東西、飛機上也能用。**

> 如果雙擊後打不開，對著檔案按右鍵 →「打開方式」→ 選 Safari 或 Chrome。

---

## 二、傳給朋友（最簡單的分享方式）

把 `玄空紫白.html` 這一個檔案傳出去就行：

- AirDrop 傳給旁邊的人
- 用 WhatsApp / Telegram / Email 當附件寄
- 丟到 Google Drive / Dropbox 給人下載

朋友收到後一樣雙擊打開。**他們也不需要安裝任何東西，也不需要網路。**

> ⚠️ WhatsApp 有時會擋 `.html` 附件。擋的話先把它壓縮成 zip 再傳，或改用 Email / Drive。

**手機上**：把檔案存到「檔案」App（iPhone）或「Files」（Android），點它 → 選用瀏覽器打開。

---

## 三、你的網站（已經上線了）

**網址：<https://u1tr4man.github.io/xuankong-zibai/>**

傳這個連結給朋友就行。倉庫：<https://github.com/U1TR4man/xuankong-zibai>

### 朋友在手機上加成 App（推薦）

- **iPhone**：用 **Safari** 開網址 → 底下「分享」→ **加入主畫面**
- **Android**：用 **Chrome** 開網址 → 右上角三點 → **加到主畫面**

桌面上會有 icon，點下去是全螢幕，**開過一次之後沒網路也能用**。

### 想要純離線單檔

<https://u1tr4man.github.io/xuankong-zibai/offline.html>

在這頁按右鍵 →「另存新檔」，存下來的就是完整離線版，之後永遠不需要網路。

### 之後想改東西

在 GitHub 網頁上直接改檔案按 Commit，或在 GitHub Desktop 改完 Commit + Push，
網站會自己重新測試、打包、發佈，大約 1–2 分鐘。

---

## 四、使用疊盤、尋星與擇吉方向

### 疊盤

1. 先在「排盤」選好日期、時間與年／月／日／時／刻。
2. 在盤名右側開啟「疊盤」。九宮每格會同時顯示年、月、日、時、刻五個數值。
3. 疊盤主星直接跟隨原有的「年／月／日／時／刻」層級列；主畫面不會再出現第二套控制。
4. 點任何一宮，例如「離 · 南」，會打開完整五層詳情及 `日時`／`時刻` 組合摘要。

組合摘要只列數字，不會自動判斷吉凶或推薦最佳時間。

### 尋星 · 簡易

1. 在頁面上方切到「尋星」。
2. 選擇開始日期、結束日期與宮位。
3. 選擇「日」「時」或「刻」，再從洛書九宮位置選一顆飛星。
4. 按「開始尋星」。所有計算都在你的裝置內完成。
5. 點任何一整列結果，App 會跳到該時間、自動開啟疊盤並高亮命中的宮。

例如選「離 · 南」「時」「九紫」，意思只會是：

> 尋找流時盤的離宮格內，飛星值等於九紫的時間。

它不是尋找「九紫入中」，也不是尋找任一宮出現九紫。

### 尋星 · 進階

按「＋ 進階條件」後，可以在流日、流時、流刻各自複選飛星；收起再展開仍會保留已選條件：

- 同一層選多顆：其中任一顆符合即可（OR）。
- 不同層有設定：每一層都必須同時符合（AND）。

第一版每次最多搜尋一年。結果很多時 App 會提示你縮短日期或增加條件，並每次顯示 50 筆；總數保持完整，不會偷偷刪除結果。

簡易尋星的日期、宮位、層級與飛星會寫進網址；重新整理、加入書籤或分享連結後都可還原。進階多層條件目前仍只保留在本次頁面使用期間。

### 紫白擇吉方向

1. 回到「排盤」，在盤頭選「擇吉」。
2. 選「通用」「文書／考試」「求財」等雙星參考用途。用途只會高亮已收錄的 tags，不會改動正式飛星結果或八方排序。
3. 九宮會同時顯示年、月、日、時四星，以及 `n/4` 的紫白集中狀態；中宮只供參考，不參與八方排序。
4. 點一個方向，可查看命中的紫白層、判斷原因、全部六組雙星與五行關係；再點雙星可開啟證據學習卡。

畫面的方向狀態是工具的 `TOOL_HEURISTIC`，不是個人化吉凶保證，也不會顯示虛構分數。有氣、墓絕與白中殺目前若顯示「尚未判定」，代表套用方法或原表仍待核對，不代表「沒有」。雙星 81 組只供學習參考，不參與方向排序；A／B／C 只是舊研究簡寫，學習卡內的證據形式、context、條件、次序可信度及原頁核對狀態才是較完整的考源資料。

### 尋組合

1. 在「尋星」頁選「尋組合」。
2. 可搜指定雙星，例如有序 `14`；也可選「不分次序」同時尋找 `14／41`。
3. 可篩選年月、年日、年時、月日、月時、日時六種 Pair Layer，或改為尋找「用途參考」。有序 pair 的第一碼是較慢層、第二碼是較快層，這是工具的時間疊盤 convention。
4. 點結果後，App 會回到該時間的擇吉盤，標出命中方向、pair 與 layer。

---

## 附錄：當初是怎麼設定的（存檔備查）

好處是有一個固定網址，朋友點連結就能用；在手機上還可以「加入主畫面」，
之後像一般 App 一樣有 icon，**而且離線也開得了**。

專案裡已經幫你設定好自動化了，你只要做下面這幾步，全部在網頁上點，不用打指令。

### 第 1 步：建一個 GitHub 帳號

到 <https://github.com> 註冊（免費）。

### 第 2 步：建一個新倉庫（repository）

1. 右上角 **+** → **New repository**
2. Repository name 填 `xuankong-zibai`（或你喜歡的名字，用英文）
3. 選 **Public**（要用免費的 GitHub Pages 必須是 Public）
4. 下面的 "Add a README file" **不要勾**
5. 按 **Create repository**

### 第 3 步：把檔案上傳

建好後的頁面上有一行小字：**uploading an existing file**，點它。

然後把你電腦上 `玄空紫白` 這個資料夾裡的東西**整包拖進去**。

> 注意：**不要**拖 `node_modules` 和 `dist` 這兩個資料夾（很大而且不需要）。
> 其他的（`src`、`tests`、`tools`、`public`、`.github`、`package.json` 之類）全部都要。
> 拖不動的話，先把資料夾壓縮成 zip 也不行 —— GitHub 要的是解開的檔案。
> 比較穩的做法是分兩次拖：先拖那些單獨的檔案，再拖各個資料夾。

拖完後拉到最下面，按 **Commit changes**。

### 第 4 步：打開 GitHub Pages

1. 倉庫頁面上方 **Settings**
2. 左邊選單找到 **Pages**
3. **Source** 那一欄，從 `Deploy from a branch` 改成 **`GitHub Actions`**

改完就好，不用按儲存。

### 第 5 步：等它自己跑完

1. 回到上方的 **Actions** 分頁
2. 會看到一個正在跑的工作（黃色轉圈）。它會自動跑測試、打包、發佈
3. 大概 1–2 分鐘後變成綠色勾勾就成功了

網址會是：

```
https://你的帳號名.github.io/倉庫名/
```

---

## 開發者才需要看的（你可以跳過）

如果哪天想在自己電腦上改：需要先裝 [Node.js](https://nodejs.org)（選 LTS 版），然後在資料夾裡開終端機：

```bash
npm install          # 第一次才要
npm run dev          # 開發模式，改檔案畫面會即時更新
npm test             # 跑 165 個測試
npm run build        # 打包網站版 → dist/
npm run build:single # 打包單檔版 → 玄空紫白.html
```

演算法規則、資料來源、怎麼新增刻盤流派，都寫在 `README.md`。
